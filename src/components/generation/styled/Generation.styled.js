import styled from 'styled-components';

export const GenAside = styled.aside`
    background: white;
    border-radius: 20px;
    max-width: 300px;
    box-shadow: 0 0 10px 4px rgba(0,0,0,0.1);
    padding: 10px 20px;
    max-height: 600px;
    overflow-y: scroll;
`